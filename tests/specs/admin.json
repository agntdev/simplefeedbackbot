[
  {
    "name": "admin panel denies access until an admin is configured",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Admin access isn't available for your account." } }]
      }
    ]
  },
  {
    "name": "photo quick action prompts for a photo",
    "steps": [
      {
        "send": { "callback": "fb:type:photo" },
        "expect": [{ "method": "editMessageText", "payload": { "text": "Send the photo you want to submit." } }]
      }
    ]
  }
]
