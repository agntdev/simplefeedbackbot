[
  {
    "name": "feedback export is denied until admin access is configured",
    "steps": [
      {
        "send": { "callback": "fb:export" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Admin access isn't available for your account." } }
        ]
      }
    ]
  }
]
